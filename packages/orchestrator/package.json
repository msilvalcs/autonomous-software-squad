{
  "name": "@squad/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src",
    "test:integration:docker": "vitest run src/docker-playwright.integration.test.ts"
  },
  "dependencies": {
    "@squad/agents": "*",
    "@squad/event-store": "*",
    "@squad/runner": "*",
    "@squad/schemas": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
